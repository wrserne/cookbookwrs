<!DOCTYPE html>
<html>
<head>
    <title>Recipe Details</title>
    <link rel="stylesheet" href="/css/styles.css">
    <style>
        /* Center-align all text */
        body {
            text-align: center;
        }

        /* Add space between Family Secrets box and the photo */
        .family-secrets-box {
            margin-top: 20px;
            margin-bottom: 20px;
            max-width: 50%; /* Set the maximum width to 50% of the parent container */
            border: 1px solid black;
            padding: 10px;
            /* Center the box horizontally */
            margin-left: auto;
            margin-right: auto;
            background-color: #f5f5f5; /* Light gray background */
        }

        /* Center the Ingredients and Instructions headings */
        h3 {
            text-align: center;
        }

        /* Center the ingredients list */
        ul {
            text-align: center;
            padding-left: 0; /* Remove the default padding to center the list items */
        }

        /* Adjust width for the instructions */
        ol {
            max-width: 70%; /* Set the maximum width for the instructions list */
            margin-left: auto; /* Center the list horizontally */
            margin-right: auto;
            text-align: left; /* Align the text to the left within the list items */
        }

        /* Reduce font size for the instructions */
        ol li {
            font-size: 14px; /* You can adjust the font size as needed */
        }

        /* Center the Family Secrets box */
        .family-secrets-box {
            margin-top: 20px;
            margin-bottom: 20px;
            max-width: 50%; /* Set the maximum width to 50% of the parent container */
            border: 1px solid black;
            padding: 10px;
            /* Center the box horizontally */
            margin-left: auto;
            margin-right: auto;
            background-color: #f5f5f5; /* Light gray background */
        }

        /* Background image styles */
        .background-image {
            background-image: url("<%= recipe.image_url %>");
            background-size: cover;
            background-repeat: no-repeat;
            background-position: center;
            min-height: 400px; /* Set a minimum height for the background image container */
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            margin-top: 20px;
            padding: 20px;
        }
    </style>
</head>
<body>
    <h1>Recipe Details</h1>
    <div class="background-image">
        <h2><%= recipe.title %></h2>
        <div class="family-secrets-box">
            <h3>Ingredients:</h3>
            <ul>
                <% recipe.ingredients.forEach(ingredient => { %>
                    <li><%- ingredient.replace(/\|/g, '<br>') %></li>
                <% }); %>
            </ul>
        </div>
        <div class="family-secrets-box">
            <h3>Instructions:</h3>
            <ol>
                <% recipe.instructions.forEach(instruction => { %>
                    <li><%- instruction.replace(/\|/g, '<br>') %></li>
                <% }); %>
            </ol>
        </div>
        <div class="family-secrets-box">
            <h3>Family Secrets:</h3>
            <p><%- recipe.family_secrets.replace(/\|/g, '<br>') %></p>
        </div>
    </div>
    <a href="/">Back to Home</a>

    <script src="/js/script.js"></script>
</body>
</html>
